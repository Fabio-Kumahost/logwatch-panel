{
  "name": "logwatch-panel",
  "version": "1.6.0",
  "description": "LogWatch Panel — centralized log collection, search and alerting for Linux fleets",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "seed-admin": "node src/db/migrate.js --seed-admin",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@fastify/jwt": "^10.1.0",
    "@fastify/rate-limit": "^10.1.1",
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.0.1",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.6.0",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "optionalDependencies": {
    "nodemailer": "^6.9.16",
    "@anthropic-ai/sdk": "^0.69.0"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  },
  "license": "MIT"
}
