# Docker Compose environment — copy to .env and fill in.
PUBLIC_URL=http://localhost:8088
# Generate: openssl rand -hex 48
JWT_SECRET=
ADMIN_USER=admin
ADMIN_PASS=
PORT=8088
RETENTION_DAYS=14
# Optional bearer token to protect GET /metrics (Prometheus)
METRICS_TOKEN=
